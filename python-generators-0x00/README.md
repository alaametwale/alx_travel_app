# Python Generators - Task 0: seed.py

This script initializes a MySQL database named `ALX_prodev`, creates a `user_data` table, and inserts data from `user_data.csv`.

## Usage
```bash
python3 0-main.py
